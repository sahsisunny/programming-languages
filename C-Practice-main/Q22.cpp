#include<iostream>
using namespace std;
int main(){
    string fullname[2];
    cout << "Enter the Full Name: ";
    cin >> fullname[0];
    cin >> fullname[1];
    cout << "Full Name: " << fullname[0] << " " << fullname[1];
}